<?php

/**
 * @file
 * Main file for custom hooks and functions.
 */

use Drupal\Core\Entity\EntityTypeInterface;

/**
 * Implements hook_entity_bundle_field_info().
 */
function bnet_oauth_entity_bundle_field_info(EntityTypeInterface $entity_type, $bundle, array $base_field_definitions) {
  if ($entity_type->id() == 'user' && $bundle == 'user') {
    // @todo field for access token.
  }
}